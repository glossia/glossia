# Pull requests

- Pull request titles must follow the semantic commit convention without a scope. Use `type: description`, such as `fix: support current Discourse proxy configuration`. Do not use scoped titles such as `fix(infra): support current Discourse proxy configuration`.
