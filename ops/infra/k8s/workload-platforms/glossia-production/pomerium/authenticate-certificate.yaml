apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authenticate-glossia-ai
  namespace: pomerium
spec:
  dnsNames:
    - authenticate.glossia.ai
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-cloudflare
  secretName: authenticate-glossia-ai-tls
