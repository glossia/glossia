# The deployment workflows read exact `name=true` lines from this file.
# Change a flag only during the corresponding maintenance-window step in
# object-storage-migration.md or cluster-consolidation.md.
production_object_storage=true
production_ceph_removed=true
observability_object_storage=true
observability_ceph_removed=true
# The compact observability release has been restored and verified on
# glossia-production (33 pods running, no Ceph, object storage on Hetzner),
# and the standalone glossia-observability cluster has been deleted, so the
# deployment workflow now targets glossia-production.
observability_on_production=true
