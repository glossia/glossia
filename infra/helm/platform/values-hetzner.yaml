# Hetzner-specific overlay for the platform chart.
# Layer on top of the base values:
#   helm upgrade --install platform infra/helm/platform \
#     -n platform --create-namespace \
#     -f infra/helm/platform/values-hetzner.yaml

ingress-nginx:
  controller:
    service:
      annotations:
        # Hetzner Cloud LB, provisioned by hcloud-cloud-controller-manager
        # which the bootstrap procedure installs on every workload cluster.
        load-balancer.hetzner.cloud/location: fsn1
        # PROXY protocol is explicitly OFF (not just absent). Cloudflare's
        # free/Pro proxy doesn't pass PROXY protocol through, so orange-
        # cloud DNS relies on HTTP-header client-IP propagation
        # (CF-Connecting-IP / X-Forwarded-For) which ingress-nginx trusts
        # via use-forwarded-headers.
        #
        # Set to "false" rather than removed: HCCM's UpdateService omits
        # the proxyprotocol field when the annotation is absent, so a
        # `helm upgrade` of a cluster whose LB was previously created with
        # uses-proxyprotocol="true" would leave the LB still prepending
        # PROXY lines to an ingress that no longer consumes them (525 /
        # TLS failures until manually patched). An explicit "false" makes
        # HCCM send proxyprotocol=false and the chart converges.
        load-balancer.hetzner.cloud/uses-proxyprotocol: "false"

# Rook and Ceph object storage for the app. This runs in the platform
# namespace because the platform chart is installed there; keep
# operatorNamespace and rookObjectStorage.clusterNamespace aligned with the
# Helm release namespace.
rook-ceph:
  enabled: true

rook-ceph-cluster:
  enabled: true
  operatorNamespace: platform
  cephClusterSpec:
    placement:
      mon:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.cluster.x-k8s.io/pool
                    operator: In
                    values:
                      - stateful
        tolerations:
          - key: node.cluster.x-k8s.io/pool
            operator: Equal
            value: stateful
            effect: NoSchedule
      mgr:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.cluster.x-k8s.io/pool
                    operator: In
                    values:
                      - stateful
        tolerations:
          - key: node.cluster.x-k8s.io/pool
            operator: Equal
            value: stateful
            effect: NoSchedule
    resources:
      mgr:
        limits:
          memory: 1Gi
        requests:
          cpu: 250m
          memory: 512Mi
      mon:
        limits:
          memory: 2Gi
        requests:
          cpu: 250m
          memory: 512Mi
      osd:
        limits:
          memory: 4Gi
        requests:
          cpu: 500m
          memory: 2Gi
    storage:
      useAllNodes: false
      useAllDevices: false
      allowOsdCrushWeightUpdate: true
      storageClassDeviceSets:
        - name: hcloud-volumes
          count: 3
          portable: true
          tuneFastDeviceClass: true
          placement:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node.cluster.x-k8s.io/pool
                        operator: In
                        values:
                          - stateful
            tolerations:
              - key: node.cluster.x-k8s.io/pool
                operator: Equal
                value: stateful
                effect: NoSchedule
          resources:
            limits:
              memory: 4Gi
            requests:
              cpu: 500m
              memory: 2Gi
          volumeClaimTemplates:
            - metadata:
                name: data
              spec:
                accessModes:
                  - ReadWriteOnce
                resources:
                  requests:
                    storage: 100Gi
                storageClassName: hcloud-volumes
                volumeMode: Block
  cephObjectStores:
    - name: glossia-s3
      spec:
        metadataPool:
          failureDomain: host
          replicated:
            size: 3
        dataPool:
          failureDomain: host
          erasureCoded:
            dataChunks: 2
            codingChunks: 1
          parameters:
            bulk: "true"
        preservePoolsOnDelete: true
        gateway:
          port: 80
          instances: 2
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 500m
              memory: 1Gi
          priorityClassName: system-cluster-critical
      storageClass:
        enabled: true
        name: glossia-s3-bucket
        reclaimPolicy: Retain
        volumeBindingMode: Immediate
        annotations: {}
        labels: {}
        parameters:
          region: us-east-1
      ingress:
        enabled: false
      route:
        enabled: false
    - name: glossia-releases-s3
      spec:
        metadataPool:
          failureDomain: host
          replicated:
            size: 3
        dataPool:
          failureDomain: host
          erasureCoded:
            dataChunks: 2
            codingChunks: 1
          parameters:
            bulk: "true"
        preservePoolsOnDelete: true
        gateway:
          port: 80
          instances: 2
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 250m
              memory: 512Mi
          priorityClassName: system-cluster-critical
        hosting:
          dnsNames:
            - releases.glossia.ai
      storageClass:
        enabled: true
        name: glossia-releases-s3-bucket
        reclaimPolicy: Retain
        volumeBindingMode: Immediate
        annotations: {}
        labels: {}
        parameters:
          region: eu-central-1
      ingress:
        enabled: false
      route:
        enabled: false

rookObjectStorage:
  enabled: true
  clusterNamespace: platform
  clusterName: rook-ceph
  objectStoreName: glossia-s3
  appNamespace: glossia
  bucketClaim:
    enabled: true
    name: glossia-s3
    bucketName: glossia-production
    storageClassName: glossia-s3-bucket
  autoscaling:
    storage:
      enabled: true
      thresholdPercent: 75
      increment: 1
      maxDeviceCount: 9
    gateway:
      enabled: true
      minReplicas: 2
      maxReplicas: 8
      cpuAverageUtilization: 65
  networkPolicy:
    enabled: true

releaseObjectStorage:
  enabled: true
  clusterNamespace: platform
  objectStoreName: glossia-releases-s3
  bucketClaim:
    enabled: true
    namespace: platform
    name: glossia-releases
    bucketName: releases.glossia.ai
    storageClassName: glossia-releases-s3-bucket
  ingress:
    enabled: true
    host: releases.glossia.ai
  networkPolicy:
    enabled: true
