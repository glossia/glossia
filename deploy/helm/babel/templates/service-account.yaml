{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "babel.serviceAccountName" . }}
  labels:
    {{- include "babel.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
