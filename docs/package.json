{
  "name": "@glossia/docs",
  "private": true,
  "type": "module",
  "devDependencies": {
    "vitepress": "1.0.0-rc.44",
    "wrangler": "^3.28.2"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name glossia-documentation --branch main"
  }
}